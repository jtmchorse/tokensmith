{
  "name": "tokensmith",
  "version": "0.1.0",
  "description": "MCP server that makes coding agents design-system-aware — look up tokens and audit code for off-system values.",
  "type": "module",
  "private": true,
  "bin": {
    "tokensmith": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "tsx": "^4.0.0",
    "typescript": "^5.4.0",
    "vitest": "^3.2.7"
  }
}
